## **眼底光凝智能教学系统 100分制评估方案**：
v1.2
### 核心评分架构总览

| 评估维度 | 核心目标 | 对应分值 | 临床意义 |
| :--- | :--- | :--- | :--- |
| **一、位置与范围** | 打在对的区域 | 35分 | 确保有效覆盖 |
| **二、激光参数空间适配度** | 参数在正确区域内是否符合专家空间策略 | 35分 | 保证治疗有效性，识别局部过强、过弱或参数选择错误 |
| **三、密度与均匀度** | 间距合理，手法稳定 | 30分 | 避免热量局部堆积或治疗死角 |

---

### 医疗事故红线（独立机制，不计入100分总分）

**"一票否决"机制**：一旦任何光斑覆盖到黄斑中心凹或视盘区域（危险区掩码内的区域），系统立即触发"严重医疗事故"红屏警告，直接结束本次模拟。

**扣分项**：若光斑覆盖到视网膜大血管区域，不直接结束本次模拟，**每有一个触碰到大血管的光斑在维度1的分数中扣5分**，作为医疗安全警示。

> 黄斑中心凹和视盘属于绝对安全红线；视网膜大血管属于高风险扣分项。

---

### 维度一：位置与范围（35分）
* **考核项：宏观靶区覆盖率（35分）**

    * **算法逻辑**：
        1. **构建玩家实际有效区域 $S_{\text{player}}$**：提取玩家所有有效光斑（排除试打点）的中心坐标，使用 **Concave Hull (凹包算法，`allow_holes=true`)** 重建玩家覆盖区域，以保留凹陷与可能的内部空洞（如环形轨迹）。  
           为减少“异常长边桥接”导致环形孔洞被填死的情况，工程实现增加两条修复策略：  
           - **多档 ratio 搜索**：在多个 ratio 档位上构建 Concave Hull，优先选择有效且拓扑合理（环形场景优先保留孔洞）的结果，降低单一 ratio 带来的退化风险。  
           - **疑似环形内点抑制**：先计算点到中心的半径分布；当检测到“少量内侧离群点”（数量不超过总点数 20%，且最多按 3 个阈值容忍）时，先剔除这批点再重建 Concave Hull。  
           当凹包结果退化为非面几何或无效几何时，回退到 **Convex Hull (凸包算法)** 作为兜底，确保评估流程稳定运行。
        2. **构建题目标准靶区 $S_{\text{target}}$**：外围轮廓减去内部挖空区域后作为实际治疗范围。
        3. **计算交并比 (IoU)**：
           
           $$ \text{IoU} = \frac{\text{Area}(S_{\text{player}} \cap S_{\text{target}})}{\text{Area}(S_{\text{player}} \cup S_{\text{target}})} $$

           - **交集 (Intersection)**：玩家覆盖范围与标准靶区的重叠面积。
           - **并集 (Union)**：玩家覆盖范围与标准靶区的总并合面积。
           - *该指标综合衡量了覆盖的“完整度” (不漏打) 与“精准度” (不打出界) 。*
    * **给分标准**：
        * IoU $\ge$ 85%：得满分 35分。
        * IoU 在 50% ~ 85% 之间：采用**线性插值**计算，具体公式为：

          $
          \text{得分} = 17.5 + \frac{\text{IoU} - 50\%}{35\%} \times 17.5
          $

          | IoU 值 | 得分 | 说明 |
          | :---: | :---: | :--- |
          | 50% | 17.5分 | 最低合格分 |
          | 60% | 22.5分 | - |
          | 67.5% | 26.25分 | 中位值 |
          | 75% | 30分 | - |
          | 85% | 35分 | 接近满分 |

        * IoU $<$ 50%：得 0分（存在大面积漏打，未达到全视网膜光凝的标准）。

### 维度二：激光参数空间适配度（35分）

* **考核目标**：不再把玩家所有光斑参数求平均后与 GT 单值比较，而是比较 **GT 参数空间分布** 与 **玩家参数空间分布**。该维度回答的问题是：玩家在已经打到正确治疗区域的地方，功率、光斑直径、曝光时间和波长是否符合专家给出的空间化参数策略。

* **评分边界**：
    * 维度一负责判断是否打在正确区域、是否漏打或越界。
    * 维度二只在 $S_{\text{target}} \cap S_{\text{player}}$ 内评价参数是否合理。
    * 维度三负责判断点位间距、密度和均匀性。

#### 2.1 GT 参数数据结构要求

GT 支持三类空间化标注来源，三者可以单独使用，也可以混合使用：

1. **专家逐光斑记录**：GT 也包含每个标准光斑的位置和参数。
2. **区域级参数标注**：医生把治疗区域划分成若干子区，每个子区给推荐参数范围和容忍阈值。
3. **参数锚点图**：医生在关键位置标注参数锚点，由系统插值生成连续参数场。

推荐 JSON 结构如下：

```json
{
  "case_id": "L3_001",
  "level": 3,
  "disease_type": "peripheral_retinal_tear",
  "target_region": {
    "mask_path": "cases/L3_001/masks/target_region.png",
    "outer_polygon": [[820, 360], [1120, 380], [1180, 620], [780, 640]],
    "holes": []
  },
  "gt_parameter_spec": {
    "source_type": "hybrid",
    "parameters": ["power", "spot_diameter", "exposure", "wavelength"],
    "expert_spots": [
      {
        "spot_id": "gt_001",
        "x": 860,
        "y": 410,
        "power": 180,
        "spot_diameter": 200,
        "exposure": 0.10,
        "wavelength": 532
      },
      {
        "spot_id": "gt_002",
        "x": 920,
        "y": 430,
        "power": 190,
        "spot_diameter": 200,
        "exposure": 0.10,
        "wavelength": 532
      }
    ],
    "region_parameter_ranges": [
      {
        "region_id": "near_tear_edge",
        "region_name_cn": "裂孔边缘区域",
        "mask_path": "cases/L3_001/masks/region_near_tear_edge.png",
        "recommended_range": {
          "power": [170, 210],
          "spot_diameter": [180, 220],
          "exposure": [0.08, 0.12],
          "wavelength": [532]
        },
        "tolerance": {
          "power": 25,
          "spot_diameter": 30,
          "exposure": 0.02,
          "wavelength": 0
        }
      },
      {
        "region_id": "outer_barrier",
        "region_name_cn": "外围屏障光凝区域",
        "mask_path": "cases/L3_001/masks/region_outer_barrier.png",
        "recommended_range": {
          "power": [160, 200],
          "spot_diameter": [180, 240],
          "exposure": [0.08, 0.13],
          "wavelength": [532]
        },
        "tolerance": {
          "power": 30,
          "spot_diameter": 40,
          "exposure": 0.03,
          "wavelength": 0
        }
      }
    ],
    "parameter_anchors": [
      {
        "anchor_id": "anchor_001",
        "x": 850,
        "y": 390,
        "note": "靠近裂孔上缘，建议能量略低",
        "value": {
          "power": 170,
          "spot_diameter": 200,
          "exposure": 0.09,
          "wavelength": 532
        }
      },
      {
        "anchor_id": "anchor_002",
        "x": 1080,
        "y": 610,
        "note": "外围区域，可使用常规能量",
        "value": {
          "power": 195,
          "spot_diameter": 200,
          "exposure": 0.10,
          "wavelength": 532
        }
      }
    ],
    "field_generation": {
      "method": "gaussian_kernel",
      "sigma_source": "spot_diameter",
      "default_sigma": 80,
      "clip_to_target_region": true
    }
  }
}
```

#### 2.2 参数场构建

对 GT 与玩家分别构建四类参数场：

$$
F_{\text{gt},k}(x,y),\quad F_{\text{player},k}(x,y)
$$

其中 $k \in \{\text{power}, \text{spot\_diameter}, \text{exposure}, \text{wavelength}\}$。

对功率、光斑直径、曝光时间三个连续参数，使用高斯核将离散光斑或锚点转换为连续参数场：

$$
F_k(x,y)=\frac{\sum_i w_i(x,y)p_{i,k}}{\sum_i w_i(x,y)}
$$

$$
w_i(x,y)=\exp\left(-\frac{d_i(x,y)^2}{2\sigma_i^2}\right)
$$

其中：

- $p_{i,k}$：第 $i$ 个光斑或锚点的第 $k$ 个参数值。
- $d_i(x,y)$：位置 $(x,y)$ 到第 $i$ 个光斑或锚点中心的欧氏距离。
- $\sigma_i$：空间影响半径，优先由光斑直径或题目配置给出。

若 GT 使用区域级参数标注，则区域内不强制生成单一 GT 数值，而是直接使用推荐范围 $[l_k(x,y), u_k(x,y)]$ 与容忍阈值 $t_k(x,y)$ 进行评分。

#### 2.3 评分区域

维度二只在 GT 靶区与玩家有效治疗区域的重合部分计算：

$$
\Omega = S_{\text{target}} \cap S_{\text{player}}
$$

原因是：

- $S_{\text{target}} - S_{\text{player}}$ 属于漏打问题，由维度一扣分。
- $S_{\text{player}} - S_{\text{target}}$ 属于越界问题，由维度一扣分。
- 维度二只评价“打到正确区域后的参数质量”。

#### 2.4 单参数误差场

对功率、光斑直径、曝光时间三个连续参数，若 GT 为连续参数场，则误差定义为：

$$
E_k(x,y)=\min\left(1,\frac{|F_{\text{player},k}(x,y)-F_{\text{gt},k}(x,y)|}{t_k(x,y)}\right)
$$

其中 $t_k(x,y)$ 是该位置的容忍阈值，由题库元数据或区域级标注给出。

若 GT 为推荐范围 $[l_k(x,y), u_k(x,y)]$，则误差定义为：

$$
E_k(x,y)=
\begin{cases}
0, & l_k(x,y) \le F_{\text{player},k}(x,y) \le u_k(x,y) \\
\min\left(1,\frac{l_k(x,y)-F_{\text{player},k}(x,y)}{t_k(x,y)}\right), & F_{\text{player},k}(x,y) < l_k(x,y) \\
\min\left(1,\frac{F_{\text{player},k}(x,y)-u_k(x,y)}{t_k(x,y)}\right), & F_{\text{player},k}(x,y) > u_k(x,y)
\end{cases}
$$

波长不按连续数值距离评分，而按离散一致性评分：

$$
E_{\text{wavelength}}(x,y)=
\begin{cases}
0, & F_{\text{player,wavelength}}(x,y) \in W_{\text{gt}}(x,y) \\
1, & F_{\text{player,wavelength}}(x,y) \notin W_{\text{gt}}(x,y)
\end{cases}
$$

其中 $W_{\text{gt}}(x,y)$ 为该位置允许使用的波长集合。若后续医生给出波长兼容矩阵，可将 $0/1$ 误差升级为分级误差。

#### 2.5 单参数得分

对每个参数在 $\Omega$ 内计算平均误差：

$$
M_k=\frac{1}{|\Omega|}\int_{\Omega}E_k(x,y)\,dx\,dy
$$

工程实现可使用像素网格或固定分辨率采样网格近似该积分。

单参数得分为：

$$
s_k = W_k \times (1 - M_k)
$$

各参数权重如下：

| 参数 | 最高分 | 评分含义 |
| :--- | :---: | :--- |
| 功率 Power | 11分 | 直接影响组织反应强弱，权重最高 |
| 光斑直径 Spot Diameter | 8分 | 影响治疗覆盖面积和能量密度 |
| 曝光时间 Exposure | 8分 | 影响热损伤程度和凝固反应 |
| 波长 Wavelength | 8分 | 影响组织吸收特性和治疗机制 |

#### 2.6 维度二总分

$$
S_{\text{维度二}} = s_{\text{power}} + s_{\text{spot\_diameter}} + s_{\text{exposure}} + s_{\text{wavelength}}
$$

$$
S_{\text{维度二}} \in [0,35]
$$

输出必须包含每个参数的子分数、平均误差、主要偏差区域和总分，便于教学复盘定位问题。

#### 2.7 热力图可视化输出

维度二应输出热力图，用于解释局部参数差异和扣分原因。推荐输出三类图：

1. **GT 参数热力图**：展示专家期望的参数空间分布。
2. **玩家参数热力图**：展示玩家实际形成的参数空间分布。
3. **误差热力图**：展示玩家与 GT 的空间偏差，是教学反馈的主要依据。

每个参数均可生成一组三图：

| 参数 | GT 图 | 玩家图 | 误差图 |
| :--- | :--- | :--- | :--- |
| 功率 | `gt_power_heatmap` | `player_power_heatmap` | `error_power_heatmap` |
| 光斑直径 | `gt_spot_diameter_heatmap` | `player_spot_diameter_heatmap` | `error_spot_diameter_heatmap` |
| 曝光时间 | `gt_exposure_heatmap` | `player_exposure_heatmap` | `error_exposure_heatmap` |
| 波长 | `gt_wavelength_map` | `player_wavelength_map` | `error_wavelength_map` |

误差热力图推荐颜色语义：

| 颜色 | 含义 |
| :---: | :--- |
| 绿色 | 接近 GT 或位于推荐范围内 |
| 黄色 | 轻度偏差 |
| 橙色 | 中度偏差 |
| 红色 | 严重偏差 |

#### 2.8 推荐评分结果 JSON

```json
{
  "dimension_2": {
    "name": "激光参数空间适配度",
    "score": 28.6,
    "max_score": 35,
    "scoring_region": "intersection_of_target_and_player_area",
    "subscores": {
      "power": {
        "score": 8.3,
        "max_score": 11,
        "mean_error": 0.25,
        "main_error_regions": ["颞下区域功率偏高"]
      },
      "spot_diameter": {
        "score": 7.2,
        "max_score": 8,
        "mean_error": 0.10,
        "main_error_regions": []
      },
      "exposure": {
        "score": 6.9,
        "max_score": 8,
        "mean_error": 0.14,
        "main_error_regions": ["裂孔上缘曝光时间偏长"]
      },
      "wavelength": {
        "score": 6.2,
        "max_score": 8,
        "mean_error": 0.22,
        "main_error_regions": ["局部区域使用了非推荐波长"]
      }
    },
    "visualization": {
      "gt_power_heatmap": "output/heatmaps/gt_power.png",
      "player_power_heatmap": "output/heatmaps/player_power.png",
      "error_power_heatmap": "output/heatmaps/error_power.png",
      "gt_spot_diameter_heatmap": "output/heatmaps/gt_spot_diameter.png",
      "player_spot_diameter_heatmap": "output/heatmaps/player_spot_diameter.png",
      "error_spot_diameter_heatmap": "output/heatmaps/error_spot_diameter.png",
      "gt_exposure_heatmap": "output/heatmaps/gt_exposure.png",
      "player_exposure_heatmap": "output/heatmaps/player_exposure.png",
      "error_exposure_heatmap": "output/heatmaps/error_exposure.png",
      "gt_wavelength_map": "output/heatmaps/gt_wavelength.png",
      "player_wavelength_map": "output/heatmaps/player_wavelength.png",
      "error_wavelength_map": "output/heatmaps/error_wavelength.png",
      "total_error_heatmap": "output/heatmaps/error_total.png"
    }
  }
}
```

### 维度三：密度与均匀度（30分）

利用空间点位分析算法，量化"打得匀不匀"和"手稳不稳"。以下计算均在同一像素坐标系下进行；$n$ 为参与统计的光斑数（与维度二一致，剔除试打）；$A$ 为题目给定的**需光凝靶区面积**（与维度一 IoU 所用靶区一致，单位取像素²）。若 $n<2$ 或 $A\le 0$，本维度相关指标可按实现约定记为不可用。

* **考核项 1：最近邻点指数 R 值（15分）**

    * **算法逻辑**（由四步得到 $R$，与"完全随机打点"的统计基线对比）：

        * **第一步：实际平均最近邻距离 $\bar{r}_1$**
            对学生打下的 $n$ 个光斑，逐个取其中心点坐标。对第 $i$ 个光斑，在其余 $n-1$ 个点中求欧氏距离最短的那一个，记该最短距离为 $d_i$（单位：像素）。对所有光斑求平均：
            $
            \bar{r}_1 = \frac{1}{n}\sum_{i=1}^{n} d_i
            $

            $\bar{r}_1$ 越小，说明光斑在局部越"挤"、越容易扎堆或重叠风险越高。

        * **第二步：点密度 $D$**
            在靶区面积 $A$ 上，光斑的面密度定义为：
            $
            D = \frac{n}{A}
            $

        * **第三步：理论最近邻距离 $\bar{r}_E$（完全随机布点的期望尺度）**
            若在面积 $A$ 内完全随机、均匀独立地撒 $n$ 个点（"盲打"基线），理论上平均最近邻距离的一个常用近似为：

            $
            \bar{r}_E = \frac{1}{2\sqrt{D}} = \frac{1}{2\sqrt{n/A}}
            $

            该值为系统提供的随机分布参照，用于和学生的实际 $\bar{r}_1$ 比较。

        * **第四步：最近邻指数 $R$**
            $
            R = \frac{\bar{r}_1}{\bar{r}_E}
            $
            即：**实际平均最近邻距离** 与 **同密度下随机布点的理论尺度** 之比。

    * **R 的直观含义**：
        * **$R < 1$（凝聚型）**：$\bar{r}_1 < \bar{r}_E$，比随机分布更"密"，易出现严重扎堆、局部热损伤风险升高 → 对应差评取向。
        * **$R \approx 1$（随机型）**：接近盲打、缺乏规律间距 → 临床教学上通常仍视为不合格/需改进（并非"均匀网格"）。
        * **$R > 1$（趋于均匀/规则）**：$\bar{r}_1 > \bar{r}_E$，比随机更"疏"、间距更有控制感；文献中专家评为高均匀的样本，其 $R$ 往往显著高于中等、低均匀组 → 对应优秀取向。

    * **给分标准**（量化阈值与系统实现绑定，可按题库标定微调）：
        * $R > 1.2$（明显优于随机、趋于均匀分布）：得 **15 分**满分。
        * $0.8 \le R \le 1.2$（接近随机分布）：得 **8 分**。
        * $R < 0.8$（凝聚型分布，光斑严重扎堆）：得 **0 分**。

* **考核项 2：光斑间距方差（15分）**

    **算法逻辑**：

    **最近邻距离方差（间距稳定性）**：
    使用考核项 1 中已算得的每个光斑的最近邻距离 $\{d_1,\ldots,d_n\}$，计算其**样本方差**：
    $$
    s_d^2 = \frac{1}{n-1}\sum_{i=1}^{n}(d_i - \bar{r}_1)^2 \quad (n\ge 2)
    $$
    $s_d^2$ 越小，说明各点与其最近邻的间距越一致，**忽密忽疏**越轻，操作越稳定。

    **给分标准**：

    预先标定两个方差阈值（均参考熟练教师或专家样本的 $s_d^2$ 分布统计得出）：
    - $\sigma_{\mathrm{good}}^2$：**优秀上限**——达到此方差以下视为操作稳定，给满分；
    - $\sigma_{\mathrm{max}}^2$：**容忍上限**——方差超过此值时视为不合格，给 0 分。

    **线性递减算法**：

    若 $s_d^2 \le \sigma_{\mathrm{good}}^2$，则该子项得满分 15 分。

    若 $\sigma_{\mathrm{good}}^2 < s_d^2 \le \sigma_{\mathrm{max}}^2$，采用**线性插值**计算得分：

    $$
    \text{得分} = 15 \times \left(1 - \frac{s_d^2 - \sigma_{\mathrm{good}}^2}{\sigma_{\mathrm{max}}^2 - \sigma_{\mathrm{good}}^2}\right)
    $$

    即方差越大，分数线性下降。整理后等价于：

    $$
    \text{得分} = 15 \times \frac{\sigma_{\mathrm{max}}^2 - s_d^2}{\sigma_{\mathrm{max}}^2 - \sigma_{\mathrm{good}}^2}
    $$

    | 方差 $s_d^2$ | 得分 | 说明 |
    | :---: | :---: | :--- |
    | $\sigma_{\mathrm{good}}^2$ | 15分 | 满分，间距最稳定 |
    | $\frac{\sigma_{\mathrm{good}}^2 + \sigma_{\mathrm{max}}^2}{2}$ | 7.5分 | 中位值 |
    | $\sigma_{\mathrm{max}}^2$ | 0分 | 容忍上限，视为不合格 |

    若 $s_d^2 > \sigma_{\mathrm{max}}^2$，则该项直接得 **0 分**（间距波动过大，操作极不稳定）。

    **阈值选择**：
    
    - 方案1-由专家样本确定:$\sigma_{\mathrm{good}}^2$ 取专家样本 $s_d^2$ 分布的第 25 百分位数；$\sigma_{\mathrm{max}}^2$ 取第 75 百分位数。
    - 方案2-由行业的专家经验值直接确定$\sigma_{\mathrm{good}}^2$和 $\sigma_{\mathrm{max}}^2$


* **附加微观惩罚：光斑重叠（Overlap）**
    * **逻辑**：如果两个光斑的中心点距离小于 1个光斑直径（即发生了物理重叠），会造成局部组织热损伤。
    * **惩罚**：每检测到一处重叠，总分扣 1分。
